## Keymap

This is the keymap for my Corne keyboard. The keymap is defined in `config/corne.keymap`.

The old keymap image is available in the `old-config` directory. A new keymap image can be generated using the `keymap-drawer` workflow.
